To run tests in dev mode, first make sure the dev build is in place:
```
npm run dev
```

Then close the app, and start the vite server:
```
npm run dev:vite
```

Then tests should work when running from VSCode, and updates to the front end code will be seen.
Note: Changes to the rust code will require recompilation.