Finish up the archive function
- Ensure every tensor type is handled
- Include predecessors as needed
    - Since the full db is not being copied, we will need to figure out how to determine lineage 
      in an archive project. Current and WIP strategies will not work with missing nodes
        - we could always just copy ALL tensorhistorynodes. Remember even for 1000 nodes, 
          it is only 1.5mb. We can also exclude the fbs for the extra entries. This would 
          provide the best compatability
        - Yeah, we'll just do that.
        - wait. wouldn't the node be copied anyway? yeah but there still might be unused ones like
          if an image was edited.
- Perhaps cache the archive plans
- Emit tauri events as archive progress
- create a function to verify the archive.

Handling:
- archive_db cache
  - should these be evicted/deleted after some time?
  - delete on exit
- fingerprints
  - maybe put a warning on the DT Project and automaticall hide its entries if the archive is present?
- extracted archives
  - these should be detected and handled the same as the zip

all of this needs to be tested. unit tests, integration tests, and e2e. It cannot be released,
even with a big warning and disclaimer, unless sufficient tests are in place. 

We will need UI for...
    - Archive settings
    x Left behind list

Checklist of things to include in archive:
- tensorhistorynode for any nodes where gen=true
- all tensordata matching above nodes
- for any tensorhistorynode that is parent of a gen...
  - all associated tensordata? might as well.

How to do it with only one pass over tensorhistorynodes?
Or more specifically, how do we know if the node we are on is a parent of a *gen*?

most parents of a gen will be a gen, so maybe we can just backtrack for those that aren't. 

FUCK IT JUST COPY EVERYTHING

yeah, at this point I don't care. There's not that much extra unless someone does a lot of paints.
